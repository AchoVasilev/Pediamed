subprojects {
    apply plugin: "java"
}

project(":server") {
    dependencies {
        implementation(project(":client"))
    }
    build.dependsOn ":server:build"
}

project(":client") {
    jar {
        from "dist/client" into "static"
    }
}